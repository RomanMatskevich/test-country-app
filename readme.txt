# clone repository on local machine

# in /api 
 -npm install 
 -npm run build
 -npm run start

# in /client
 -npm install
 -npm run build
 -npm start

client env:
NEXT_PUBLIC_BACKEND_URL="http://localhost:3000"

NEXT PORT: 3001
NEST PORT: 3000